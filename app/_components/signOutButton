import { signOutAction } from "@/app/_lib/actions";


export default function SignOutButton() {
    return (
        <form action={signOutAction}>
            <button className="text-xs text-on-surface-variant hover:text-on-surface transition-colors duration-200" type="submit">Sign out</button>
        </form>
    )
}